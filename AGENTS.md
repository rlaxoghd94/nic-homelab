## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for `rlaxoghd94/nic-homelab`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the canonical triage-state labels alongside existing type labels such as `bug`, `enhancement`, and `documentation`. See `docs/agents/triage-labels.md`.

### Domain docs

This repository uses a single-context domain documentation layout. See `docs/agents/domain.md`.
